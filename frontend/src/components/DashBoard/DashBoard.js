import React from 'react';
import AppBar from './AppBar';

const DashBoard = React.memo(() => {

    return (
        <div>
            <AppBar  />
        </div>
    );
});

export default DashBoard;